{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://alistigo.ai/schema/v1/document.json",
  "title": "Alistigo Document",
  "description": "A self-contained Alistigo list document in JSON-LD ItemList format.",
  "type": "object",
  "required": [
    "@context",
    "@type",
    "alistigo:listId",
    "alistigo:schemaVersion",
    "itemListElement",
    "alistigo:listEventLog"
  ],
  "properties": {
    "@context": {
      "type": "object",
      "required": ["@vocab", "alistigo"],
      "properties": {
        "@vocab": { "type": "string", "const": "https://schema.org/" },
        "alistigo": { "type": "string", "const": "https://alistigo.ai/vocab/" }
      }
    },
    "@type": { "type": "string", "const": "ItemList" },
    "alistigo:listId": { "type": "string", "pattern": "^lst_" },
    "alistigo:schemaVersion": { "type": "string", "pattern": "^\\d+\\.\\d+\\.\\d+$" },
    "name": { "type": "string" },
    "itemListElement": {
      "type": "array",
      "items": { "$ref": "#/$defs/ListItem" }
    },
    "alistigo:listEventLog": {
      "type": "array",
      "items": { "$ref": "#/$defs/EventRecord" }
    },
    "alistigo:actors": {
      "type": "array",
      "items": { "$ref": "#/$defs/ActorRecord" }
    },
    "alistigo:plugins": {
      "type": "array",
      "items": { "$ref": "#/$defs/PluginRecord" }
    }
  },
  "$defs": {
    "ListItem": {
      "type": "object",
      "required": ["@type", "alistigo:listElementId", "position", "name"],
      "properties": {
        "@type": { "type": "string", "const": "ListItem" },
        "alistigo:listElementId": { "type": "string", "pattern": "^lse_" },
        "position": { "type": "integer", "minimum": 1 },
        "name": { "type": "string", "minLength": 1 },
        "alistigo:metadatas": {
          "type": "object",
          "additionalProperties": { "type": "object" }
        }
      }
    },
    "EventRecord": {
      "type": "object",
      "required": [
        "alistigo:listEventId",
        "alistigo:eventType",
        "alistigo:listId",
        "alistigo:actorId",
        "alistigo:timestamp"
      ],
      "properties": {
        "alistigo:listEventId": { "type": "string" },
        "alistigo:eventType": {
          "type": "string",
          "enum": [
            "ListCreated",
            "ListElementAdded",
            "ListElementDeleted",
            "ListExported",
            "ListElementChecked"
          ]
        },
        "alistigo:listId": { "type": "string" },
        "alistigo:actorId": { "type": "string" },
        "alistigo:timestamp": { "type": "string", "format": "date-time" },
        "alistigo:listElementId": { "type": "string" },
        "checked": { "type": "boolean" }
      }
    },
    "ActorRecord": {
      "type": "object",
      "required": [
        "alistigo:actorId",
        "alistigo:userId",
        "alistigo:pseudo",
        "alistigo:avatar"
      ],
      "properties": {
        "alistigo:actorId": { "type": "string" },
        "alistigo:userId": { "type": "string" },
        "alistigo:pseudo": { "type": "string" },
        "alistigo:avatar": { "type": "string" }
      }
    },
    "PluginRecord": {
      "type": "object",
      "required": ["name"],
      "properties": {
        "name": { "type": "string" },
        "version": { "type": "string" },
        "config": { "type": "object" }
      }
    }
  }
}
