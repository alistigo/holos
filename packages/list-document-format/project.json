{
  "name": "list-document-format",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/list-document-format/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "packages/list-document-format/dist",
        "tsConfig": "packages/list-document-format/tsconfig.json",
        "main": "packages/list-document-format/src/index.ts",
        "rootDir": "packages/list-document-format/src"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun test",
        "cwd": "packages/list-document-format"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json --noEmit",
        "cwd": "packages/list-document-format"
      }
    },
    "qa:lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "biome check .",
        "cwd": "packages/list-document-format"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf dist",
        "cwd": "packages/list-document-format"
      }
    }
  }
}
