{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "name": "claude-artifact-api",
  "sourceRoot": "packages/claude-artifact-api/src",
  "projectType": "library",
  "targets": {
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json --noEmit",
        "cwd": "packages/claude-artifact-api"
      }
    },
    "qa:lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "biome check src/",
        "cwd": "packages/claude-artifact-api"
      }
    }
  }
}
