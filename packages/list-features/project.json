{
  "name": "list-features",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/list-features/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "@nx/js:tsc",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "packages/list-features/dist",
        "tsConfig": "packages/list-features/tsconfig.json",
        "main": "packages/list-features/src/index.ts",
        "rootDir": "packages/list-features/src"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json --noEmit",
        "cwd": "packages/list-features"
      }
    },
    "qa:lint": {
      "executor": "nx:run-commands",
      "options": {
        "parallel": true,
        "commands": ["bun ./node_modules/gherklin/bin/gherklin", "biome check ."],
        "cwd": "packages/list-features"
      }
    },
    "format": {
      "executor": "nx:run-commands",
      "options": {
        "commands": ["prettier --write 'features/**/*.feature'", "biome check . --write"],
        "cwd": "packages/list-features"
      }
    },
    "format:check": {
      "executor": "nx:run-commands",
      "options": {
        "command": "prettier --check 'features/**/*.feature'",
        "cwd": "packages/list-features"
      }
    },
    "validate": {
      "executor": "nx:run-commands",
      "options": {
        "command": "bun tools/validate.ts",
        "cwd": "packages/list-features"
      }
    },
    "clean": {
      "executor": "nx:run-commands",
      "options": {
        "command": "rm -rf dist",
        "cwd": "packages/list-features"
      }
    }
  }
}
