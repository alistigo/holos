---
name: Update Gherkin runner to target new iframe page URL
status: open
created: 2026-06-11T14:20:20Z
updated: 2026-06-11T14:20:20Z
github: (will be set on sync)
depends_on: [005]
parallel: false
conflicts_with: []
---

# Task: Update Gherkin runner to target new iframe page URL

## Description

The Gherkin runner (`packages/alistigo-features-runner/`) currently targets the root URL
of `alistigo-list-embedded-app`. After the M2 restructure, the list artifact lives on the
`/iframe.html` page of `alistigo-artifact-playground`. Update the runner to point to the
new URL.

## Acceptance Criteria

- [ ] Runner base URL updated from the old app root to `http://localhost:<port>/iframe.html`
- [ ] URL is configurable via env var (`ALISTIGO_IFRAME_URL` or similar) — not hardcoded
- [ ] All `@m1` Gherkin scenarios in `packages/alistigo-features/features/core/` still pass with the updated URL
- [ ] `project.json` `test` target for the runner is updated if it references the old app name

## Technical Details

The Playwright driver likely navigates to a URL defined in a config or constant. Find that
reference (search for `localhost` or `alistigo-list` in `packages/alistigo-features-runner/`)
and update it.

If the runner currently spins up the app itself (via `preview` or `dev` server), update
the server target path to `apps/alistigo-artifact-playground`.

## Dependencies

- Task 005: `alistigo-artifact-playground` must exist with the iframe page at `/iframe.html`

## Effort Estimate

- Size: XS
- Hours: 1–2
