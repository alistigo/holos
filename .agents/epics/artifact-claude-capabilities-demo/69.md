---
name: Rename packages + update all references
status: open
created: 2026-08-06T20:14:46Z
updated: 2026-08-06T20:14:46Z
github: https://github.com/alistigo/holos/issues/69
depends_on: []
parallel: false
conflicts_with: []
---

# Task: Rename packages + update all references

## Description

Rename the two existing packages from `artifact-storage-explorer` / `artifact-storage-explorer-skill` to `artifact-claude-capabilities-demo` / `artifact-claude-capabilities-demo-skill`. Update all references across the monorepo.

## Acceptance Criteria

- [ ] `packages/artifact-storage-explorer/` directory renamed to `packages/artifact-claude-capabilities-demo/`
- [ ] `packages/artifact-storage-explorer-skill/` renamed to `packages/artifact-claude-capabilities-demo-skill/`
- [ ] Both `package.json` names updated to new `@alistigo/` scoped names
- [ ] `project.json` names updated in both packages
- [ ] `packages/artifact-manager/src/registry.ts` entry updated (key, cdnUrl, skillPackage fields)
- [ ] `packages/artifact-core-components-react` Tailwind scan path updated if it references old dir
- [ ] `pnpm install` runs clean (workspace references resolved)
- [ ] Old epic `claude-storage-explorer` marked as superseded in its epic.md

## Technical Details

Files to update:
- `packages/artifact-claude-capabilities-demo/package.json` — name: `@alistigo/artifact-claude-capabilities-demo`
- `packages/artifact-claude-capabilities-demo/project.json` — name: `artifact-claude-capabilities-demo`
- `packages/artifact-claude-capabilities-demo-skill/package.json` — name: `@alistigo/artifact-claude-capabilities-demo-skill`
- `packages/artifact-claude-capabilities-demo-skill/project.json` — name: `artifact-claude-capabilities-demo-skill`
- `packages/artifact-manager/src/registry.ts` — update the `@alistigo/artifact-storage-explorer` key and values
- `packages/artifact-core-components-react/vite.config.ts` — check if it scans old path

Also update `ARTIFACT_NAME` constant in `App.tsx` (now `src/` of the renamed package).

Run `grep -r "artifact-storage-explorer" packages/ apps/ --include="*.json" --include="*.ts" --include="*.tsx" --include="*.md"` to find any remaining references.

## Dependencies

None — this is the foundation task.

## Effort Estimate

- Size: M
- Hours: 2

## Definition of Done

- [ ] Code implemented
- [ ] `pnpm install` runs without errors
- [ ] `pnpm nx run artifact-claude-capabilities-demo:build` succeeds
- [ ] No remaining references to `artifact-storage-explorer` in source files (grep check)
- [ ] Code reviewed
