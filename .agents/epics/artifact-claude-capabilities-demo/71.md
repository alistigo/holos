---
name: Tab navigation shell + wire Storage tab
status: open
created: 2026-08-06T20:14:46Z
updated: 2026-08-06T20:14:46Z
github: https://github.com/alistigo/holos/issues/71
depends_on: [69]
parallel: false
conflicts_with: []
---

# Task: Tab navigation shell + wire Storage tab

## Description

Restructure the renamed artifact's `App.tsx` to use a tabbed layout with 5 tabs. Build the `Tabs.tsx` component with Tailwind. Extract existing storage UI into `StorageTab.tsx`. Leave the other 4 tabs as placeholders so the build passes.

## Acceptance Criteria

- [ ] `Tabs.tsx` component renders a horizontal tab bar with: Storage | AI | File Generation | API Calls | External Navigation
- [ ] Active tab is highlighted with a bottom border or background
- [ ] `StorageTab.tsx` contains the exact same draft overlay + `StorageExplorerApp` behavior as the current `App.tsx`
- [ ] `App.tsx` is simplified to: lifecycle → loading/error screens → `<Tabs>` with 5 tab contents
- [ ] Draft overlay appears ONLY in the Storage tab, not the whole artifact
- [ ] `DraftBadge` remains in `ArtifactContextMenuContainer` (visible from all tabs)
- [ ] Other 4 tabs render a `<div>Coming soon</div>` placeholder (to be replaced in T004–T007)
- [ ] Build passes: `pnpm nx run artifact-claude-capabilities-demo:build`

## Technical Details

### Tabs.tsx

```tsx
type Tab = 'storage' | 'ai' | 'file-generation' | 'api-calls' | 'external-navigation';

interface TabsProps {
  active: Tab;
  onChange: (tab: Tab) => void;
  children: Record<Tab, ReactNode>;
}
```

Tab labels: Storage, AI, File Generation, API Calls, External Navigation.

Style: simple horizontal nav, Tailwind-only, no dependency on any component library.

### App.tsx restructure

- Move draft detection (`isDraft`) to `App.tsx`, pass as prop to `StorageTab`
- Move `DraftBadge` / `DraftWarningModal` into `StorageTab.tsx`
- `ArtifactContextMenuContainer` in `App.tsx` gets a `statusBadge` prop only when `isDraft` is true (keep existing behavior)
- Storage startup check (`claudeStoragePlugin.storage?.isAvailable()`) stays in `useStartArtifact` — if storage is unavailable we still show ErrorScreen for the whole artifact (this case means it's not in a Claude conversation at all)

## Dependencies

- Depends on 001 (renamed package must exist)

## Effort Estimate

- Size: M
- Hours: 3

## Definition of Done

- [ ] Code implemented
- [ ] Build passes
- [ ] Storage tab behavior identical to current artifact (verified in playground)
- [ ] Other tabs show placeholder text
- [ ] Code reviewed
