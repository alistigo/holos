---
name: Update M3 PRD to reference @alistigo/artifact-plugin-api
status: open
created: 2026-07-09T09:57:47Z
updated: 2026-07-09T10:10:17Z
github: https://github.com/alistigo/holos/issues/22
depends_on: []
parallel: true
conflicts_with: []
---

# Task: Update M3 PRD to reference @alistigo/artifact-plugin-api

## Description

`.agents/prds/alistigo-ai-m3.md` currently reserves package names `alistigo-plugin-api`/
`alistigo-plugin-checkbox` for its own "Plugin Architecture & First Plugin" concept
(domain/element-type plugins like a future `checkbox-element` plugin). Since this epic
builds one unified `@alistigo/artifact-plugin-api` interface meant to serve both this
epic's infra plugins and M3's future domain-contribution plugins, M3's PRD needs a
follow-up edit so the two don't end up as competing, duplicate packages.

## Acceptance Criteria

- [ ] `.agents/prds/alistigo-ai-m3.md`'s F1 ("Plugin interface: data shape + render + commands + events") edited to reference `@alistigo/artifact-plugin-api` (from `packages/alistigo-artifact-plugin-api`) instead of reserving a second `alistigo-plugin-api` package
- [ ] The PRD's "Deliverables" section's `packages/alistigo-plugin-api/` entry removed or redirected to note it's superseded by `@alistigo/artifact-plugin-api`
- [ ] A short note added to the M3 PRD cross-referencing this epic (`alistigo-artifact-plugins`) and its ADR (`docs/adrs/0016-artifact-plugin-system.md`, once task 008 lands) as the source of the now-shared interface
- [ ] `packages/alistigo-plugin-checkbox` naming is left untouched (it's still a distinct future package for the checkbox plugin itself — only the *interface* package name is being deduplicated)
- [ ] `docs/milestones.md` §M3 spot-checked for the same stale package name and updated if present

## Technical Details

This is a documentation-only task — no code changes. Read `.agents/prds/alistigo-ai-m3.md`
and the §M3 section of `docs/milestones.md` in full before editing, and preserve all
other PRD content and frontmatter (only bump `updated`, per the ccpm skill's edit
convention — note this PRD template doesn't currently have an `updated` field; add one
if editing conventions call for it, otherwise leave frontmatter as-is).

## Dependencies

None — can happen anytime, independent of the rest of the epic's implementation order.

## Effort Estimate

- Size: XS
- Hours: 1

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
