---
name: Playground - plugin-selection checkbox UI
status: open
created: 2026-07-09T09:57:47Z
updated: 2026-07-09T10:10:17Z
github: https://github.com/alistigo/holos/issues/19
depends_on: [14, 17]
parallel: false
conflicts_with: []
---

# Task: Playground — plugin-selection checkbox UI

## Description

Add a dynamic plugin-selection control to `apps/alistigo-artifact-playground`'s Dev
Config Form: a checkbox list whose available options depend on the currently selected
artifact type (`config.app`). Selected plugins (with their, for now empty, `{}`
per-plugin config) must serialize into the config passed to the mounted artifact.

## Acceptance Criteria

- [ ] A plugin-availability map exists mapping artifact type → available plugin package names (e.g. extend `ARTIFACT_REGISTRY` entries in `packages/alistigo-artifact-manager/src/registry.ts` with an `availablePlugins: string[]` field, or add a sibling map in the playground app — pick one, document the choice in the PR/commit)
- [ ] `useHostConfig.ts`'s `Config` type gains a `plugins?: Record<string, Record<string, unknown>>` field (or equivalent selected-plugin-names state, converted to the record shape at serialization time)
- [ ] `HostForm.tsx`: a checkbox list rendered below the existing artifact-type selector, populated from the availability map for `config.app`, re-rendering when `config.app` changes (existing precedent: the `readonly` checkbox is already conditionally shown per `config.app === "@alistigo/artifact-list"` — follow that pattern, generalized)
- [ ] Selected plugins serialize into a single JSON-encoded query param (e.g. `?plugins=%7B...%7D`) via `HostPage.tsx::buildIframeSrc()`
- [ ] `main.tsx` (iframe-side) decodes that query param and writes it into the `plugins` field of the `#alistigo-config` JSON tag, alongside the existing `app`/`lang`/`readonly` fields
- [ ] Round-trip verified: checking a plugin checkbox, reloading the iframe (via the existing "Reload" button), and confirming the plugin is still selected in the form and still present in the resulting config
- [ ] `pnpm build && pnpm build:typecheck` pass for the playground app
- [ ] `pnpm nx qa:lint` passes

## Technical Details

Read `apps/alistigo-artifact-playground/src/hooks/useHostConfig.ts`,
`src/components/HostForm.tsx`, `src/components/HostPage.tsx`, and `src/main.tsx` fully
before starting — the existing URL-param serialization in `buildIframeSrc()` is flat
key-value; `plugins` is the first nested-object field this pipeline has to carry, hence
the JSON-encode-into-one-param approach rather than inventing a second config-delivery
path.

## Dependencies

- Task 002 (`plugins` field shape in config-format, for type alignment)
- Task 005 (`alistigo-artifact-list`'s `mount()` must actually accept/use `options.plugins` for this UI to have any effect)

## Effort Estimate

- Size: M
- Hours: 5–6

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
