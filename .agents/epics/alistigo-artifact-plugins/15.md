---
name: Scaffold @alistigo/artifact-sentry-plugin
status: open
created: 2026-07-09T09:57:47Z
updated: 2026-07-09T10:10:17Z
github: https://github.com/alistigo/holos/issues/15
depends_on: [13]
parallel: true
conflicts_with: []
---

# Task: Scaffold @alistigo/artifact-sentry-plugin

## Description

Create `packages/alistigo-artifact-sentry-plugin` (npm `@alistigo/artifact-sentry-plugin`),
implementing `AlistigoPlugin` from `@alistigo/artifact-plugin-api`. This replaces the
Sentry logic currently in `packages/alistigo-artifact-list/src/monitoring.ts` and the
`@sentry/react` `ErrorBoundary` wrapping in `mount.ts` — but as a pure `setup()` +
event-subscriber, with **no React dependency at all**.

## Acceptance Criteria

- [ ] Package scaffolded, depends on `@alistigo/artifact-plugin-api` (type-only import) and `@sentry/browser`
- [ ] Default export is an `AlistigoPlugin` object: `name: "@alistigo/artifact-sentry-plugin"`
- [ ] `setup(ctx)`: reads DSN from this plugin's own build-time env var (`VITE_SENTRY_DSN`, baked in at this plugin's own publish time — same posture as today's `monitoring.ts`), no-ops silently if absent; otherwise calls `Sentry.init({ dsn, release: \`${ctx.host.packageName}@${ctx.host.version}\`, environment: ctx.host.environment, tracesSampleRate: 0 })`; idempotency guard so repeated `setup()` calls don't re-init
- [ ] `setup(ctx)` also calls `ctx.on("error:uncaught", ({ error }) => Sentry.captureException(error))`
- [ ] No `@sentry/react` dependency, no React import
- [ ] Own `vite.config.ts`: bundled dependency-free ESM output (`build.lib.formats: ["es"]`, nothing externalized — `@sentry/browser` must be bundled in, not left as a bare import)
- [ ] Own `@sentry/vite-plugin` sourcemap-upload wiring (moved from `alistigo-artifact-list`'s build config), using this plugin package's own `SENTRY_ORG`/`SENTRY_PROJECT`/`SENTRY_AUTH_TOKEN` publish-time env vars
- [ ] Unit tests: `setup()` is a no-op without a DSN; `setup()` initializes Sentry exactly once across repeated calls; the `error:uncaught` handler calls `Sentry.captureException` with the received error (mock `@sentry/browser`)
- [ ] `pnpm build && pnpm build:typecheck` pass; built `dist/index.js` contains no bare `import` specifiers (verify by inspecting output or a build-time check)
- [ ] `pnpm nx qa:lint` passes

## Technical Details

Reference the existing `packages/alistigo-artifact-list/src/monitoring.ts` for the exact
`Sentry.init()` options and idempotency-guard pattern to preserve (release string format,
`tracesSampleRate: 0`, environment from `import.meta.env.MODE`). Reference
`packages/alistigo-artifact-list/vite.config.ts` for the existing `@sentry/vite-plugin`
sourcemap-upload configuration to relocate here.

## Dependencies

- Task 001 (`@alistigo/artifact-plugin-api` must exist for the `AlistigoPlugin`/`PluginContext` types)

## Effort Estimate

- Size: M
- Hours: 4–5

## Definition of Done
- [ ] Code implemented
- [ ] Tests written and passing
- [ ] Code reviewed
