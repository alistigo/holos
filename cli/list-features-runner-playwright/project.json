{
  "name": "list-features-runner-playwright",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "cli/list-features-runner-playwright/src",
  "projectType": "application",
  "targets": {
    "run-all-features": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build"],
      "options": {
        "command": "bun ./node_modules/@cucumber/cucumber/bin/cucumber.js",
        "args": "--config cucumber.config.ts --profile all",
        "cwd": "cli/list-features-runner-playwright"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "dependsOn": ["^build", { "projects": ["alistigo-artifact-playground"], "target": "build" }],
      "options": {
        "command": "bash -c 'set -m; nx run alistigo-artifact-playground:dev & until curl -sf http://localhost:5173 > /dev/null; do sleep 1; done && nx run alistigo-features-runner-playwright:run-all-features; RESULT=$?; kill %1 2>/dev/null; wait %1 2>/dev/null; exit $RESULT'"
      }
    },
    "install-browsers": {
      "executor": "nx:run-commands",
      "options": {
        "command": "node_modules/.bin/playwright install chromium",
        "cwd": "cli/list-features-runner-playwright"
      }
    },
    "typecheck": {
      "executor": "nx:run-commands",
      "options": {
        "command": "tsc -p tsconfig.json --noEmit",
        "cwd": "cli/list-features-runner-playwright"
      }
    },
    "qa:lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "biome check .",
        "cwd": "cli/list-features-runner-playwright"
      }
    }
  }
}
